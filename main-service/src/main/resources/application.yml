server:
  port: 8090
  servlet:
    context-path: /main

spring:
  application:
    name: main-service
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://192.168.121.151:3306/scf?serverTimezone=Asia/Shanghai&autoReconnect=true&autoReconnectForPools=true&useUnicode=true&characterEncoding=UTF-8
      username: root
      password: Cango24@
      type: com.zaxxer.hikari.HikariDataSource
      hikari:
        minimum-idle: 5
        maximum-pool-size: 15
        auto-commit: true
        idle-timeout: 30000
        pool-name: creditHikariCP
        max-lifetime: 1800000
        connection-timeout: 30000
        connection-test-query: SELECT 1

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.oycl.mainservice.orm.model
  global-config:
    db-column-underline: true
    capital-mode: false
    meta-object-handler: cango.scf.com.credit.common.config.MetaObjectHandlerConfig
  configuration:
    map-underscore-to-camel-case: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/



