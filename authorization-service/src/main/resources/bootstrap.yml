spring:
    profiles:
      active: @profiles.active@

encrypt:
#  对称加密
  key: oW-B6~t>Vgf0
---
spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
  application:
    name: authorization-service
  profiles: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.17.32.10:8660/eureka/
  instance:
      #服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
      lease-expiration-duration-in-seconds: 30
      #服务刷新时间配置，每隔这个时间会主动心跳一次
      lease-renewal-interval-in-seconds: 10
---
spring:
  cloud:
    config:
      label: master
      profile: prod
      discovery:
        enabled: true
        serviceId: config-server
  application:
    name: authorization-service
  profiles: prod

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.17.32.5:8660/eureka/,http://172.17.32.7:8660/eureka/
  instance:
      #服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
      lease-expiration-duration-in-seconds: 30
      #服务刷新时间配置，每隔这个时间会主动心跳一次
      lease-renewal-interval-in-seconds: 10
---
spring:
  cloud:
    config:
      label: master
      profile: stage
      discovery:
        enabled: true
        serviceId: config-server
  application:
    name: authorization-service
  profiles: stage

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.17.32.12:8660/eureka/
  instance:
      #服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
      lease-expiration-duration-in-seconds: 30
      #服务刷新时间配置，每隔这个时间会主动心跳一次
      lease-renewal-interval-in-seconds: 10
---
  spring:
    cloud:
      config:
        label: master
        profile: test
        discovery:
          enabled: true
          serviceId: config-server
    application:
      name: authorization-service
    profiles: test

  eureka:
    client:
      serviceUrl:
        defaultZone: http://172.17.32.11:8660/eureka/,http://172.17.32.14:8660/eureka/
        #defaultZone: http://172.17.32.11:8660/eureka/
    instance:
        #服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
        lease-expiration-duration-in-seconds: 30
        #服务刷新时间配置，每隔这个时间会主动心跳一次
        lease-renewal-interval-in-seconds: 10

---

spring:
  cloud:
    config:
      label: master
      profile: local
      discovery:
        enabled: true
        serviceId: config-server
  application:
    name: authorization-service
  profiles: local
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8660/eureka/
  instance:
      #服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
      lease-expiration-duration-in-seconds: 30
      #服务刷新时间配置，每隔这个时间会主动心跳一次
      lease-renewal-interval-in-seconds: 10